route isFactor(i : int,val : int) : bool
{
	ret ((i mod val) == 0);
}

route PrimeTest(nothing) : int
{
	def inputVal : int;
	print("Enter a number to check:\n");
	inputVal = in();
	if (isPrime(inputVal))
	{
		def ifDefTest : int; 
		print("Is prime!\n");
	}
	else
	{
		def elseDefTest : int; 
		print("Not prime!\n");
	}
	ret 0;
} 

route print(nothing) : int
{
	ret 0;
}

route in(nothing) : int
{
	ret 0;
}

route isPrime(num : int) : bool
{
	def i : int;
	def z : char;
	def m : dbl;
	m = z + i;
	i = 2;
	def res : bool;
	def name : string;
	res = true;
	while (i < num - 1 AND res <> false) do
	{
		def whileDefTest : int; 
		if (isFactor(i,num))
		{
			def whileIfDefTest : int; 
			res = false;
		}
	}
	ret res;
}
